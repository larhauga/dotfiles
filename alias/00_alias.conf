# Aliases
    alias ls='ls --color'
    alias ll='ls -l'
    alias grep='grep --color=auto'
    alias zgrepaccess="zgrep -Ev 'gif|jpg|skin|ico|css|Images|Image|PortalHealthCheck'"

    alias config=". ~/.bashrc"
    #alias enc=""
    #alias dec=""
    alias dropbox="$HOME/.dropbox/dropbox.py"

    alias topmem="ps aux | sort -b -k 4 | tail | grep -v USER"
    alias topcpu="ps aux | sort -nrk 3 | head | grep -v USER"

