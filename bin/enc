#! /bin/bash

if [ -z "$1" ]; then
    echo "Usage: $0 <filename>"
else
    if [ -f $(which openssl) ]; then
        $(which openssl) aes-256-cbc -a -salt -in $1 -out $1.enc
    else
        echo "No openssl on the system"
    fi
fi
